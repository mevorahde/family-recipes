{
  "name": "family-recipes-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/vision": "6.0.0",
    "cheerio": "^1.2.0",
    "firebase-admin": "14.3.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
